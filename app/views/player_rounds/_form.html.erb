<%= form_with(model: player_round, local: true) do |form| %>
  <% if player_round.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_round.errors.count, "error") %> prohibited this player_round from being saved:</h2>

      <ul>
      <% player_round.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :player_round_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
